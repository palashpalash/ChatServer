spring.datasource.url=jdbc:mysql://localhost:3306/palash
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

spring.session.store-type=jdbc
server.port=8080


#create table Users(userid integer NOT NULL auto_increment primary key  ,username varchar(20),password1 varchar(20),Firstname varchar(20),Lastname varchar(20));

#create table allmessage (


	
	#messageid	int(11)		PRIMARY Key		auto_increment,
	#sender	int(11)				,
	#reciever	int(11)			,	
	#message	varchar(4000)		,		
	#actulatime	timestamp		,		
	#sendershown	varchar(10)		,		
	#recievershown	varchar(10)	,			
	#fileshown	varchar(4)	
    #);
